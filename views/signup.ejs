<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <style>
        body {
            background-color: #6c829a; /* Navy blue background */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #ecf0f1; /* Light grey text */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            justify-content: center;
        }

        h1 {
            color: #ecf0f1; /* Light grey text */
            margin-bottom: 20px;
        }

        div {
            background-color: #34495e; /* Darker navy blue background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            width: 400px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 10px;
            color: #ecf0f1; /* Light grey text */
        }

        input[type="text"],
        input[type="password"] {
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #7f8c8d; /* Light grey border */
            border-radius: 5px;
            background-color: #ecf0f1; /* Light grey background */
            color: #2c3e50; /* Navy blue text */
        }

        button {
            background-color: #2980b9; /* Blue button */
            color: #ecf0f1; /* Light grey text */
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #1a5276; /* Darker blue on hover */
        }
    </style>
</head>
<body>
    <h1>SignUp</h1>
    <div>
        <form action="/user" method="post">
            <label>Full Name :</label>
            <input type="text" required name="name">
            <label>E-mail :</label>
            <input type="text" required name="email">
            <label>Password :</label>
            <input type="password" required name="password">

            <button type="submit">SignUp</button>
        </form>
    </div>
</body>
</html>
