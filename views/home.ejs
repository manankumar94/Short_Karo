<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Short_Karo 🔗</title>
    <style>
        body {
            background-color: #6c829a; /* Navy blue background */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #ecf0f1; /* Light grey text */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            justify-content: center;
        }

        h1 {
            color: #ecf0f1; /* Light grey text */
            margin-bottom: 20px;
        }

        p {
            color: #ecf0f1; /* Light grey text */
            background-color: #34495e; /* Darker navy blue background */
            padding: 10px;
            border-radius: 5px;
            text-align: center;
        }

        div {
            background-color: #34495e; /* Darker navy blue background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            width: 400px;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 10px;
            color: #ecf0f1; /* Light grey text */
        }

        input[type="text"] {
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #7f8c8d; /* Light grey border */
            border-radius: 5px;
            background-color: #ecf0f1; /* Light grey background */
            color: #2c3e50; /* Navy blue text */
        }

        button {
            background-color: #2980b9; /* Blue button */
            color: #ecf0f1; /* Light grey text */
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #1a5276; /* Darker blue on hover */
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #7f8c8d; /* Light grey border */
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #2980b9; /* Blue header background */
            color: #ecf0f1; /* Light grey text */
        }

        td {
            background-color: #34495e; /* Darker navy blue background */
            color: #ecf0f1; /* Light grey text */
        }

        thead {
            background-color: #2980b9; /* Blue background for the table header */
        }
    </style>
</head>
<body>
     <h1><u>URL Shortener</u></h1>
     <% if (locals.id) { %>   
        <p>URL Generated : http://localhost:<%= port %>/<%= id  %></p>
     <% } %>
     <div>
        <form method="POST" action="/url">
            <label>Enter Your Original URL</label>
            <input type="text" name="url" placeholder="https://example.com">
            <button type="submit">Generate</button>
         </form> 
     </div> 
     <div>
        <% if (locals.urls) { %>
         <table>
            <thead>
                <tr>
                    <th>S.No</th>
                    <th>ShortID</th>
                    <th>Redirect_URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach((url, index) => { %>
                <tr>
                    <td><%= index+1 %></td>
                    <td><%= url.ShortId %></td>
                    <td><%= url.redirectURL %></td>
                    <td><%= url.visitHistory.length %></td>
                </tr>
                <% }) %>
            </tbody>
         </table>
        <% } %>
     </div>
</body>
</html>
